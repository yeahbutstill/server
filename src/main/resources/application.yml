spring:
  datasource:
    password: PNSJkxXvVNDAhePMuExTBuRR
    url: jdbc:mysql://${MYSQL_HOST:localhost}:6603/serverdb
    username: servermanager
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
