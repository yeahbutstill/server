spring:
  datasource:
    password: PNSJkxXvVNDAhePMuExTBuRR
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/serverdb
    username: servermanager
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
